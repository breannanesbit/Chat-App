events { }
http {
   include mime.types;
   types {
      application/wasm;
    }
    server {
        listen 80;
        server_name localhost;

        location / {
           proxy_pass http://client:4001
        }
    }
}

